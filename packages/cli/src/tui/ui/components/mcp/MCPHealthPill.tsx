/**
 * MCPHealthPill — DeepCode stub.
 *
 * Qwen Code's footer pill summarizing MCP server health. DeepCode manages MCP
 * in the runtime without this footer indicator; the pill renders nothing.
 */

export function MCPHealthPill(): null {
  return null;
}
