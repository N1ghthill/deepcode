/**
 * FeedbackDialog — DeepCode stub.
 *
 * Qwen Code's in-product feedback survey is not part of DeepCode. The dialog
 * renders nothing and `FEEDBACK_DIALOG_KEYS` is empty, so the feedback key
 * branches in ported components stay inert.
 */

/** Keys the feedback dialog would intercept. Empty in DeepCode. */
export const FEEDBACK_DIALOG_KEYS: readonly string[] = [];

export function FeedbackDialog(): null {
  return null;
}
